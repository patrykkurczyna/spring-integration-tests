server:
  port: 8080

management:
  server:
    port: 8081
  endpoints:
    web:
      base-path: /_
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
      show-components: always

payment:
  url: https://example.com
